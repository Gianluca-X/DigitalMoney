server:
  port: 8082

spring:
  application:
    name: auth-service
  datasource:
    url: jdbc:mysql://localhost:3306/auth_service_db
    username: auth_service_user
    password: nerea
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.MySQLDialect
  config:
    import: "optional:configserver:"
  mail:
    host: smtp.gmail.com
    port: 587
    username: moneydigitaldh@gmail.com
    password: malrmttkridclkrr
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true
          timeout: 5000
        debug: true
eureka:
instance:
    hostname: auth-service
    instance-id: ${spring.application.name}:${spring.application.instance_id:${random.value}}
    service-url:
      defaultZone: http://localhost:8761/eureka
    statusPageUrlPath: /actuator/info
    healthCheckUrlPath: /actuator/health

jwt:
  secret: MySuperSecretKeyWith32Bytes!!
  expiration: 86400000 # 1 día en milisegundos
logging:
  level:
    org.hibernate.type.descriptor.sql: TRACE
    org.springframework: INFO
    com.zaxxer.hikari: DEBUG

rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest
    virtual-host: /
